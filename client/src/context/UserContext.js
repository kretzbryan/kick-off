import { createContext } from "react";

//this function will set up a context for us

//React Context allows us to use state  local to a scope of components
//in this case, the whole app/

//for the moment stores no data.
export default createContext(null);